# Data (the CSVs are large and not part of the code)
data/*.csv
*.zip

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Notebooks
.ipynb_checkpoints/

# Testing / linting
.pytest_cache/
.coverage
htmlcov/

# MLflow
mlruns/
mlflow.db

# OS / editors
.DS_Store
Thumbs.db
.vscode/
.idea/

# Regenerated by the notebook when run locally (already committed at the repo root)
flight_price_app/
flight_model/
